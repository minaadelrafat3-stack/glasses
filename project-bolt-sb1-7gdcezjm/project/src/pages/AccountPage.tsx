import { PagePlaceholder } from '@/components/shared';

export function AccountPage() {
  return (
    <PagePlaceholder
      badge="Account"
      title="Your account"
      description="Sign in, order history, and saved face profile will live here. The auth context and profile structure are already wired up."
    />
  );
}
